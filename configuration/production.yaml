application:
  host: 0.0.0.0
database:
  require_ssl: true

email_client:
  # Value retrieved from Mailchimp's API documentation
  base_url: "https://api.postmarkapp.com"
  # Use the single sender email you authorised on Mailchimp!
  sender_email: "sangkhuu@rustdev.online"  